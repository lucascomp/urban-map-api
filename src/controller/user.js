const getUser = (ctx) => {
  ctx.body = {
    ok: 'ok',
  };
};

const createUser = (ctx) => {
  ctx.body = {
    ok: 'ok',
  };
};

const updateUser = (ctx) => {
  ctx.body = {
    ok: 'ok',
  };
};

const login = (ctx) => {
  ctx.body = {
    ok: 'ok',
  };
};

const logout = (ctx) => {
  ctx.body = {
    ok: 'ok',
  };
};

module.exports = {
  getUser,
  createUser,
  updateUser,
  login,
  logout,
};
